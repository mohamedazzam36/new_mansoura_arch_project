import 'package:flutter/material.dart';

/// Warm architectural color palette for New Mansoura project.
/// Inspired by cream parchment, aged wood, and burnished copper tones.
class AppColors {
  AppColors._();

  // --- Primary Palette ---
  static const Color cream = Color(0xFFF5EFE6);
  static const Color creamLight = Color(0xFFFAF7F2);
  static const Color creamDark = Color(0xFFE8DDD0);

  // --- Brown / Coffee Tones ---
  static const Color brownDeep = Color(0xFF3D1F0D);
  static const Color brownPrimary = Color(0xFF6B3F2A);
  static const Color brownMedium = Color(0xFF8B5E3C);
  static const Color brownLight = Color(0xFFB8835A);
  static const Color brownAccent = Color(0xFFD4A96A);

  // --- Text ---
  static const Color textPrimary = Color(0xFF2C1810);
  static const Color textSecondary = Color(0xFF5A3D2B);
  static const Color textMuted = Color(0xFF8B7355);
  static const Color textOnDark = Color(0xFFF5EFE6);

  // --- UI Elements ---
  static const Color divider = Color(0xFFD4C4B0);
  static const Color shadow = Color(0x33000000);
  static const Color overlay = Color(0x99000000);
  static const Color cardBackground = Color(0xFFFDF9F4);

  // --- App Bar ---
  static const Color appBarBackground = Color(0xFF2C1810);
  static const Color appBarText = Color(0xFFF5EFE6);

  // --- Footer ---
  static const Color footerBackground = Color(0xFF1A0F07);
  static const Color footerText = Color(0xFFD4C4B0);
  static const Color footerLink = Color(0xFFD4A96A);
}
